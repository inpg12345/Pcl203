 	
Zobacz podsumowanie

#include <stdio.h>

main()
{
int srednia, a=75, b=9;(średnik)

srednia=a/b;(średnik)
printf("Sredni czas na wykonanie jednego zadania wynosi %d\n:", srednia);(średnik)
}

 

#include <stdio.h>

main()
{
float srednia, a=75, b=9;(średnik)

srednia=a/b;(średnik)
printf("Sredni czas na wykonanie jednego zadania wynosi %f\n:", srednia);(średnik)
}
